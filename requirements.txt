# Tested with the versions in the comments; older ones likely work too.
numpy          # 2.2.6
scipy          # 1.15.3
matplotlib     # 3.10.3  (only needed for --plot)
numpy-stl      # 3.2.0
pillow         # 11.2.1  (only needed for --image)
